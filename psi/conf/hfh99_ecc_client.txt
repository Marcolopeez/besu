# 服务端信息
server_name = server
server_ip = 127.0.0.1
server_port = 9002

# 客户端信息
client_name = client
client_ip = 127.0.0.1
client_port = 9003

# 所属参与方
own_name = client

# protocol type
pto_type = PSI

# element byte length
element_byte_length = 16

# parties log set size (log(n))
server_log_set_size = 12,10
client_log_set_size = 12,10

# protocol name
psi_pto_name = HFH99_ECC

# specific config
compress_encode = false
append_string = _uncompress
